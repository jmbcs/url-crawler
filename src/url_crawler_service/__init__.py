"""Crawl service: HTTP API, worker and Postgres storage around the url_crawler core."""

SERVICE_EXTRA_HINT = (
    "the crawl service needs the 'service' extra: pip install 'url-crawler[service]'"
)
